spring:
  redis:
    host: localhost
    database: 0
    port: 6379
    lettuce:
      pool:
        min-idle: 5
        max-idle: 10
        max-active: 8
        max-wait: 1ms
        shutdown-timeout: 100ms